```
1.小程序主题目录结构
	pages 项目所有组件
		index.wxml -xml文件(非常严格)
	app.js 入口程序
	app.json 项目全局配置文件
			 (1)不能加注释
			 (2)内容严格
	app.wxss  全局样式
			  添加二个新功能  (1)单位: rpx (2)@import x.wxss 
```

```
2.小程序-wxml
	小程序wxml是一种xml文件用于描述网页结构
	xml-格式要求非常严格
		2.1一个xml文件必须有且只有一个根元素
		<books>
		....
		</books>
		2.2所有标签有开始必须有结束
		2.3所有属性值必须双引号 <button name="btn">
	#不是html
	
	wxml常用标签：
	<view></view>  相当于<div></div>
	<text></text>  相当于<span></p>
	<image></image> 相当于 <img src=""/>
	<navigator></navigator> 相当于<a>
	<form></form>
```

```
3. wxml-image组件 
   3.1 图片路径
	1.本地绝对路径  /pages/img/1.png
	2.相对路径     ../img/blue.png
	3.网络绝对路径   http://127.0.0.1:3000/blue/png
   3.2 懒加载图片 默认false
   	  如果当前图片是通过ajax异步获取 false-true
   	  <image  lazy-load></image>
   3.3 图片默认宽度和高度 300px*225px
   3.4 mode 缩放
   		aspectFit 保持纵横比缩放图片，使图片的长边能完全显示出来。也就是说，可以完整地将图片显示出来。
   		aspectFill  保持纵横比缩放图片，只保证图片的短边能完全显示出来。也就是说，图片通常只在水平或垂直方向是完整的，另一个方向将会发生截取。
```

```
4.小程序wxss(样式文件)
	每一个组件中都有一个样式默认样式自动加载wxml
	不用手工引入
	小程序对css文件封装操作
	#rpx (r响应式)
	开发项目:(1)设计师ps标准图片(效果图 px [物理像素]
	前端开发:(2)开发 网页(图片) px(逻辑像素)
	示例:
	iphone6准设计图宽度(物理像素750px)    2x
	        (逻辑像素375px)(逻辑像素750rpx)
	ihone6plus 					    3x
	注意:width;height使用rpx font-size:12px
	#样式引入
	@import 引入其他样式文件
	#小程序样式布局:弹性布局
```

```
5.小程序 js文件
	小程序一个组件对应.js 默认有一个对象
	Page({data:{},onload:function()...})
	小程序js程序
	(1)ECMAScript
		基本数据类型  numbers string boolean null undefined
		引用类型	 Object Number Date RegExp 
		
	(2)小程序没有DOM/BOM概念 
    (3)小程序有一个顶级js对象 wx(类似window)
    	查询文档:小程序API
```

```
6.小程序 组件
  轮播图:
  <swiper>
  	<swiper-item>
  		<image src="">
  	</swiper-item>
  </swiper>
  常见属性: 
  	-autoplay 自动播放
	-interval 播放间隔
	-indictor-dots 指示点
	-vertical  方向
```

```
7.小程序生命周期
	全局生命周期 app.js 
		-onLaunch 当小程序运行只执行一次
	局部生命周期(组件生命周期)
		-(1)onLoad 组件加载时候触发,一个页面只会调用一次
			参数:options 不同组件之间传递参数
			a?id=10  b:options.id
			建议:通常在此时间发送ajax 开启定时器
		-(3)onReady 页面初次渲染完成触发,一个页面只会调用一次
				 代表页面已渲染结束可以进入交互操作
		-(2)onShow 页面显示或者切换到前台时触发
		-onHide 页面隐藏/切换后台触发
		-onUnload 页面卸载时触发 通过程序跳转其他组件
				  redirectTo()  navigateBack() 触发
		-onPullDownRefresh 监听用户下拉操作 
						   默认小程序禁止用户下拉操作
						   在全局配置文件enablePullDownRefresh设置为true
		-onReachBottom 	   监听用户上拉触底操作
		-onPageScroll({})  监听用户滑动页面事件
				scrollTop  页面在垂直方向已经滚动距离				
```

```
8.小程序配置
	全局配置项目
		app.json			全局
		  pages: array      页面路径列表  默认显示第一个组件内容
		  window			窗口样式(渲染方式)
		  	navigationBarTextStyle:"black"/"white"
		  	"navigationBarBackgroundColor" 背景颜色 "#f00"  red错误
		  	-tarbar  	 兼容性差
		exam01/exam01.json 	局部(就近原则)
	局部配置项目
```

```
9.小程序事件
	事件分类:冒泡事件 非冒泡事件
	1.冒泡事件:当一个组件上的时间被触发后,该事件会向父节点传递
	2.非冒泡事件:当一个组件上的事件被触发后,该事件不会向父节点传递
```

```
10.跳转
	保留当前页面
	wx.navigateTo({
      url: '/pages/list/list'
    })
    不保留当前页面
    wx.redirectTo({
      url: 'test?id=1'
    })
```

```
11.事件委托的原理
	事件委托是利用事件的冒泡原理来实现的，何为事件冒泡呢？就是事件从最深的节点开始，然后逐步向上传播事件，举个例子：页面上有这么一个节点树，div>ul>li>a;比如给最里面的a加一个click点击事件，那么这个事件就会一层一层的往外执行，执行顺序a>li>ul>div，有这样一个机制，那么我们给最外面的div加点击事件，那么里面的ul，li，a做点击事件的时候，都会冒泡到最外层的div上，所以都会触发，这就是事件委托，委托它们父级代为执行事件。
	
```

```
12.小程序本地收藏逻辑
	1.收藏多条新闻时 不能用单个键值对来存 应该用对象来存
	2.初始化的时候 第一次什么都没有收藏的时候 也就是对应isCollected为false的时候
	  给data初始化一个对象
	3.当点击收藏的时候 先从storage里获取原先的obj对象 然后在obj对象基础上往里面追加
```

